name: Workflow1
on: [push]

# gh runner images
jobs:
  runs-shell-commands:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "hello world"
      - name: echo a string
        run: | 
          node -v
          npm -v


